name: Deploy Next.js to AWS S3

on:
  push:
    branches:
      - master

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20.9.0

      - name: Install dependencies
        run: npm install

      - name: Install dependencies
        run: npm install --save-dev @types/node

      - name: Build Next.js
        run: npm run build

      - name: Export static site
        run: npm run export

      - name: Deploy to S3
        uses: jakejarvis/s3-sync-action@master
        with:
          args: --delete
        env:
          AWS_S3_BUCKET: ${{ secrets.production.S3_BUCKET_NAME }}
          AWS_ACCESS_KEY_ID: ${{ secrets.production.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.production.AWS_SECRET_ACCESS_KEY }}
          AWS_REGION: ${{ secrets.production.AWS_REGION }}
          SOURCE_DIR: out